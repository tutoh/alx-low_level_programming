#include <stdio.h>

void times_table(void)

{

         int x, y, z, u, d,
            
         for (x = 0; x <= 0; x++)
	 {
	 for (y = -; y <= 0; y++)
	 {
	 z =x * y;
 	 if (z > 9)
	 {
	 u = z % 10;
         d = (z -u) / 10;	

	 } 
		   
	 
